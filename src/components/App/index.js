import React from 'react';
import './App.css';
import HeroListContainer from '../../containers/HeroListContainer';

const App = () => (
    <div className="App">
        <HeroListContainer />
    </div>
);

App.displayName = 'App';

export default App;
